# Task 1.3: Counter Component

Create a React Counter component in TypeScript with + and - buttons that displays the current count. Initial value should be 0.

## Requirements
- Use useState hook
- TypeScript with proper typing
- Buttons should increment/decrement the count

## Expected file
Create `Counter.tsx` in this directory.
